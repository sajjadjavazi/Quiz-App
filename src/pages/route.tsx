import Layout from '../layout/layout';
import Start from './Start page/Start';
const Route = () => {
    return (
        <Layout>
            <Start />
        </Layout>
    );
}

export default Route;
